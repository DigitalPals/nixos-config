#!/usr/bin/env bash
set -euo pipefail

official=/usr/bin/lumen
compat=/opt/xps-builds/lumen-compat/current/bin/lumen
installed_version=$(rpm -q --qf '%{VERSION}' lumen 2>/dev/null || true)

if [[ $installed_version == 0.7.6 && -x $compat ]]; then
  exec "$compat" "$@"
fi

exec "$official" "$@"
