#!/usr/bin/env bash
set -euo pipefail
cd -- "$(dirname -- "${BASH_SOURCE[0]}")"
./verify --pre-finalize
exec ansible-playbook site.yml --tags finalize \
  -e gdm_autologin=true -e manual_hyprland_verified=true "$@"
